import styled from "styled-components";

export const GenericPageContainer = styled.main`
	display: flex;
	flex-direction: column;
	justify-content: center;
	align-items: center;
	gap: 1rem;

	padding: 1rem 2rem;
`;
